.lineas(layout="row" layout-align="center center")
    .lineas-content(flex="90")
        .busqueda
            md-autocomplete(
                flex
                md-selected-item="ctrl.lineas.item"
                md-selected-item-change="ctrl.lineas.seleccion(item)"
                md-search-text="ctrl.lineas.busqueda"
                md-search-text-change="ctrl.lineas.buscar(ctrl.lineas.busqueda)" 
                md-items="item in ctrl.lineas.filtrar(ctrl.lineas.busqueda)" 
                md-item-text="item.nombre"
                placeholder="Nombre")
                md-item-template
                    span {{item.nombre}}
            md-button(ng-click="ctrl.lineas.crear()"  md-whiteframe="1" )
                md-icon add
        md-card
            md-table-container
                table(md-table md-row-select multiple)
                    thead(md-head)
                        tr(md-row)
                            th(md-column md-numeric) Nombre
                            th(md-column)
                    tbody(md-body)
                        tr(md-row md-select="dato" md-select-id="name" md-auto-select ng-repeat="item in ctrl.lineas.items")
                            td(md-cell) {{item.nombre}}
                            td(md-cell)
                                md-fab-toolbar(md-open="false" count="0"  md-direction="left" flex)
                                    md-fab-trigger
                                        md-button
                                            md-icon more_vert
                                    md-toolbar
                                        md-fab-actions(layout="row")
                                            md-button(ng-click="item.ir()")
                                                md-icon edit
                                                span Editar
            cl-paging(cl-pages="ctrl.lineas.filtros.paginas" cl-steps="6" cl-page-changed="ctrl.lineas.obtener()" cl-align="start start" cl-current-page="ctrl.lineas.filtros.pagina")
